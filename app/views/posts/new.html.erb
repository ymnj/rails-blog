<h1>Create a post</h1>
<h2>This should be on new branch</h2>
 
<p><%= flash[:notice] %></p>

<%= form_for @post do |f| %>
	<div>
		<%= f.label :title %>
		<%= f.text_field :title %>	
	</div>
	<div>
		<%= f.label :body %>
		<%= f.text_area :body %>
	</div>
	<%= f.submit %>
<% end %>

